# OT Trust — BL-141 fixture depth (DNP3)

Date: 2026-09-06  
Branch tip: `cursor/ot-trust-bl141-dnp3`

## Goal

Continue BL-141 depth after Modbus UDP (#153): add a stdlib **DNP3** TCP outstation peer for TOP-20 `dnp3`. Wire subset matches the Java loopback (`Dnp3TcpCodec` / `Dnp3LoopbackOutstation`) integrity poll. **Poll-only** (ADR-0057) — no write smoke.

## Added fixture

| Service | Port | Smoke | Notes |
|---------|------|-------|-------|
| `dnp3` | TCP `127.0.0.1:20000` | Integrity poll → AI0 / BI0 / CTR0 | Master **1**, outstation **1024**; seed AI0=**12.34**, BI0=**true**, CTR0=**999** |

## Self-tests (no docker)

```bash
deploy/tools/driver-interop-smoke.sh --self-test-dnp3
```

## TOP-20 compose coverage (after this change)

| Layer | Covered |
|-------|---------|
| Docker compose fixtures | **11 / 20** (`mqtt`, `modbus-tcp`, `modbus-udp`, `opcua`, `snmp`, `http`, `bacnet`, `iec104`, `ethernet-ip`, `dlms`, `dnp3`) |
| Fixture smoke | **11 / 20** |
| Gradle interop modules in CI | **20 / 20** (unchanged) |

## Deferred / next

- ~~**S7**~~ — done — see [2026-09-06-bl141-s7-fixture.md](2026-09-06-bl141-s7-fixture.md)
- **modbus-rtu** — needs PTY/serial pair
- **gps-tracker** — driver is the TCP listener (awkward peer shape)
- Plant soaks for BL-140 field Done

## Honesty

Lab docker fixtures ≠ field certification / OT scorecard 10/10 / BL-140 plant Done. This peer speaks the ISPF clean-room DNP3 subset (single-segment CRC) — not OpenDNP3 / full IEEE 1815.
