# OT Trust — BL-141 fixture depth (IEC 104)

Date: 2026-09-06  
Branch tip: `cursor/ot-trust-bl141-iec104`

## Goal

Continue BL-141 depth after BACnet (#149): add a stdlib **IEC 60870-5-104** TCP outstation peer for TOP-20 `iec104`. No separate peer for `iec104-server` (that driver *is* the slave).

## Added fixture

| Service | Port | Smoke | Notes |
|---------|------|-------|-------|
| `iec104` | TCP `127.0.0.1:2404` | `C_SE_NC_1` + `C_RD_NA_1` on IOA **3001** (FLOAT) | CA=1; seed **42.5** as `M_ME_NC_1`; STARTDT/TESTFR U-frames |

## Self-tests (no docker)

```bash
deploy/tools/driver-interop-smoke.sh --self-test-iec104
```

## TOP-20 compose coverage (after this change)

| Layer | Covered |
|-------|---------|
| Docker compose fixtures | **7 / 20** (`mqtt`, `modbus-tcp`, `opcua`, `snmp`, `http`, `bacnet`, `iec104`) |
| Fixture smoke | **7 / 20** |
| Gradle interop modules in CI | **20 / 20** (unchanged) |

## Deferred / next

- **S7** — needs external soft PLC (not stdlib)
- **EtherNet/IP** / **DLMS** — when a stable OSS peer exists
- **DNP3** — poll-only (ADR-0057); lower write-smoke value
- Plant soaks for BL-140 field Done

## Honesty

Lab docker fixtures ≠ field certification / OT scorecard 10/10 / BL-140 plant Done.
