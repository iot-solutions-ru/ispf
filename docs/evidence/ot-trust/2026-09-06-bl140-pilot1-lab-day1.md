# BL-140 Pilot #1 — lab OT VLAN day 1

Date: 2026-09-06 (UTC)  
Site: **`lab-ot-vlan-192.168.100`** (`192.168.100.10` / `spark-1f58`)  
Pilot id: `pilot1-modbus-plant`

## Honesty

| Claim | Status |
|-------|--------|
| Named site (C1) | **Filled** — internal OT lab VLAN (not docker-only on a laptop) |
| ready-for-field | **Yes for this named lab site** |
| BL-140 field Done | **No** — need days 2–7 + OT sign-off |
| Competitive OT 10/10 | **No** |
| Customer plant | **No** — internal lab soak |

## What was stood up

| Component | Detail |
|-----------|--------|
| ISPF | `ispf-enterprise-l` on `:8080` (0.9.207) with `ISPF_DRIVER_PACKS_DIR=/opt/ispf/drivers` mounted |
| Modbus peer | `192.168.100.10:1502` — stdlib TCP slave; systemd user unit `ispf-pilot1-modbus-peer.service` |
| Device | `root.platform.devices.pilot1-modbus.plant` |
| Driver | `modbus-tcp` → host `172.17.0.1` (docker bridge to host) / port `1502` |
| Tags | 50 holding registers `hr00`…`hr49` → `1:HOLDING:0`…`49` |
| Historian | Enabled on `hr00`…`hr04` (30d, CHANGES_ONLY) |

## Validation §1 results

| Check | Result | Evidence |
|-------|--------|----------|
| Connect / RUNNING | ✅ | `connected=true`, `status=RUNNING` |
| Read ≥50 tags | ✅ | `hrCount=50`; sample `hr01=10`, `hr49=490` |
| Write | ✅ | write `hr00` → `1234`; read-back confirms |
| Historian top 5 | ✅ | `historyEnabled=true` on hr00–hr04 |
| HMI / mimic | ⏸ | Not exercised day 1 (API/driver path only) |
| Stale / disconnect (C5) | ⏸ | Scheduled during soak window |

Raw JSON: [pilot1-lab/day1-2026-09-06.json](pilot1-lab/day1-2026-09-06.json)

## Lab notes / follow-ups

1. **Driver config bug (lab pack):** `modbus-tcp` pack `readConfig` only looked at device variables named `host`/`port`, not `driverConfigJson` / `configuration()`. Day-1 workaround: create `host`/`port`/`timeoutMs` variables. Source fix lands with this PR (`configuration()` first, then variables).
2. Enterprise-L container was recreated with driver packs mounted (previous run had API-only jar → empty `/api/v1/drivers`).
3. Peer must stay up for soak (`systemctl --user enable --now ispf-pilot1-modbus-peer`).

## Next

- Daily journal rows days 2–7  
- C5 disconnect once  
- Optional HMI screenshot  
- Sign-off only after day 7  
