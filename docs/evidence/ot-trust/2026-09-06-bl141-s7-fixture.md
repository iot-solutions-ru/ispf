# OT Trust — BL-141 fixture depth (S7 SoftPlc)

Date: 2026-09-06  
Branch tip: `cursor/ot-trust-bl141-s7`

## Goal

Continue BL-141 depth after DNP3 (#154): add an external **SoftPlc** (Snap7) peer for TOP-20 `s7`. ISO-on-TCP `:102` matches ISPF defaults (`host=127.0.0.1`, rack **0**, slot **2**, s7connector port **102**). Smoke seeds/verifies demodata DB1 REAL via SoftPlc REST (same memory Snap7 serves). Lab ≠ field Done.

## Added fixture

| Service | Port | Smoke | Notes |
|---------|------|-------|-------|
| `s7` (`fbarresi/softplc:latest-linux`) | TCP `127.0.0.1:102` (ISO-on-TCP) + HTTP `127.0.0.1:8080` (REST) | SoftPlc REST write/read REAL @ DB1 offset **80** | `DATA_PATH=/demodata`; point map `DB:1:80:REAL`; no in-container healthcheck (host waits TCP+API) |

## Self-tests (no docker)

```bash
deploy/tools/driver-interop-smoke.sh --self-test-s7
```

## Honesty / scope

- SoftPlc REST proves the **same DB memory** the ISO-on-TCP peer serves; a full pure-Python S7Comm client is out of stdlib scope.
- Java **s7connector** matrix / loopback remains the ISO-on-TCP codec proof.
- SoftPlc ≠ Siemens PLC / plant certification / OT scorecard 10/10 / BL-140 field Done.

## TOP-20 compose coverage (after this change)

| Layer | Covered |
|-------|---------|
| Docker compose fixtures | **12 / 20** (`mqtt`, `modbus-tcp`, `modbus-udp`, `opcua`, `snmp`, `http`, `bacnet`, `iec104`, `ethernet-ip`, `dlms`, `dnp3`, `s7`) |
| Fixture smoke | **12 / 20** |
| Gradle interop modules in CI | **20 / 20** (unchanged) |

## Deferred / next

- **modbus-rtu** — needs PTY/serial pair (current `modbus/server.py` is MBAP-only)
- **gps-tracker** — driver is the TCP listener (awkward peer shape)
- Optional later: `python-snap7` wire round-trip if CI install is reliable
- Plant soaks for BL-140 field Done
