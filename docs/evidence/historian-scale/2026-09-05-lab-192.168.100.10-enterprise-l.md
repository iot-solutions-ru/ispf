# Enterprise L lab result — 192.168.100.10

| Field | Value |
|-------|-------|
| When (UTC) | 2026-09-05T06:08:48Z |
| Jump | `84.42.21.226:5031` → LAN `192.168.100.10` |
| ISPF | **0.9.207** container `ispf-enterprise-l` (Spring profile `local` + Postgres/ClickHouse) |
| History-enabled under `root.platform.devices.analytics-scale-lab.*` | **50 000** |
| ClickHouse `ispf.variable_samples` rows | **1 000 023 688** |
| Multi-tag query | 10 tags × 7d × 1h — **p95 ≈ 49 ms** (SLO &lt; 3000 ms) → **PASS** |

## Method / honesty

- Catalog: API seed (`seed-analytics-scale-catalog.py --tags 50000`).
- Samples: **synthetic** ClickHouse `INSERT … FROM numbers()` (TTL removed for lab retention). **Not** live MQTT plant ingest.
- Demostand (`ispf.iot-solutions.ru`) was **not** seeded to 50k.
- Lab credentials stay **out of git** (operator vault / chat only).
- `GET …/history-enabled-count` returns 404 on this 0.9.207 jar until the tooling PR is deployed; count verified via PG + API walk (**50 000**).

## Artifacts

- [multi-tag SLO JSON](./2026-09-05-lab-192.168.100.10-multi-tag-slo.json)
- On-lab notes: `~/ispf/ENTERPRISE_L_RESULT.md` on `192.168.100.10`
