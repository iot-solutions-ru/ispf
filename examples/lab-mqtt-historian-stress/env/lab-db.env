# Anonymized lab template — copy to ~/ispf on your hosts and replace placeholders.
# Do not put real hosts, SSH users, or passwords in git. See local/README.md.
ISPF_LAB_DB_HOST=198.51.100.10
ISPF_LAB_DB_BIND=198.51.100.10
ISPF_DB_PASSWORD=CHANGE_ME_POSTGRES

# Scylla — 20 cores on loadgen host; leave RAM for CH + emqtt
SCYLLA_SMP=20
SCYLLA_MEMORY=40G

# ClickHouse historian/journal tests
CLICKHOUSE_PASSWORD=CHANGE_ME_CLICKHOUSE
CLICKHOUSE_MEM_LIMIT=32g
