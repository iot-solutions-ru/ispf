# Sample licensed driver pack manifest (FW-50)

Copy to `${ISPF_DRIVER_PACKS_DIR}/<pack-name>/` with the signed JAR.

See [docs/en/licensed-driver-packs.md](../../docs/en/licensed-driver-packs.md) for the full contract.

Sign the JAR hash with `tools/license-builder/` (adapt signing payload to driver-pack license fields).
