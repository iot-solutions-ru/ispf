# ERP-MES CMMS

CMMS overlay on [`erp-mes-core`](../erp-mes-core/) maintenance lite (≥ **2.1.0**).

```bash
python examples/erp-mes-cmms/generate_bundle.py
```

Spare parts, failure codes, PM plans → `emc_maint_createRequest`, WO labor/parts issue.
