# Lab cluster nginx — edge-api ingress (replica-1/2) + analytics paths on replica-3.
# replica-4 (io/drivers) is internal — not in nginx upstream.
upstream ispf_edge_backend {
    least_conn;
    server ispf-server-1:8080 max_fails=2 fail_timeout=10s;
    server ispf-server-2:8080 max_fails=2 fail_timeout=10s;
}

upstream ispf_analytics_backend {
    server ispf-server-3:8080 max_fails=2 fail_timeout=10s;
}

upstream ispf_ws_backend {
    ip_hash;
    server ispf-server-1:8080 max_fails=3 fail_timeout=30s;
    server ispf-server-2:8080 max_fails=3 fail_timeout=30s;
}

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 64m;

    # Materializer health (analytics replica only)
    location /api/v1/platform/analytics/rollups/materializer/ {
        proxy_pass http://ispf_analytics_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Authorization $http_authorization;
        proxy_read_timeout 180s;
        proxy_connect_timeout 5s;
    }

    location /api/ {
        proxy_pass http://ispf_edge_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Authorization $http_authorization;
        proxy_read_timeout 180s;
        proxy_connect_timeout 5s;
        proxy_next_upstream error timeout invalid_header http_502 http_503 http_504;
        proxy_next_upstream_tries 3;
        proxy_next_upstream_timeout 10s;
    }

    location /ws/ {
        proxy_pass http://ispf_ws_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 86400;
    }

    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
