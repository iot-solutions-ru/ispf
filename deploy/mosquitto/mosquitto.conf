listener 1883
# Lab only — do not expose on public networks without TLS (see mosquitto.prod.conf.example).
allow_anonymous true
persistence false
# Lab load tests: broker ingress counter ($SYS/broker/messages/received)
sys_interval 1
