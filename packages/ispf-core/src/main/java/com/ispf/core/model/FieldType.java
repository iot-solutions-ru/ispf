package com.ispf.core.model;

/**
 * Strongly-typed field types for {@link DataRecord} schemas.
 * Field types for structured data records.
 */
public enum FieldType {
    BOOLEAN,
    INTEGER,
    LONG,
    DOUBLE,
    STRING,
    DATETIME,
    BINARY,
    RECORD,
    RECORD_LIST
}
