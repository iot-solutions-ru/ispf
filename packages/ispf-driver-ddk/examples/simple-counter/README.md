# Simple Counter (DDK reference)

Poll-only driver that increments an in-memory counter per mapped variable. Copy to `packages/ispf-driver-simple-counter/` to build a standalone pack.

Point mapping: any non-blank string (e.g. `counter:main`).
