# JSON Poller (DDK reference)

Poll-only driver stub for HTTP JSON endpoints. Point mapping: `jsonPath:$.field.name`.

Copy to `packages/ispf-driver-json-poller/` and wire real HTTP fetch in `readPoints`.
