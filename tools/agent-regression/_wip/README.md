# WIP scenarios (not in BL-178 gate)

Park incomplete scenario JSON here so `validate-scenarios.mjs` / `AgentLiveRegressionSuiteTest` only see `scenarios/`.

`scada-ot-automation-excellence.json` — ADR-0049 OT automation tools; re-add under `scenarios/` when `invoke_workflow_tool` / analytics tools are stable in the live suite.